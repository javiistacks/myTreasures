<main>
        <div class="p-3">
            <h2 class="text-center text-light">Featured Shoes</h2>
            <div class="container animate__animated animate__fadeInUp">
            <ol class="container ">
            {{#each shoes as |shoe|}}
            {{>inventory-cards}}
            {{/each}}
            </ol>    
            </div> 
            </div>
        </div>
    </main>